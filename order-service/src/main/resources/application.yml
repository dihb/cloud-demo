server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/data-order?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: Firedove9@
  application:
    name: orderservice
mybatis:
  mapper-locations: classpath:mapping/*.xml
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9090/eureka